import { TelefoneMaskDirective } from './telefone-mask.directive';

describe('TelefoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefoneMaskDirective();
    expect(directive).toBeTruthy();
  });
});
